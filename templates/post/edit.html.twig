{% extends 'base.html.twig' %}

{% block title %}Edit Post{% endblock %}

{% block body %}

<div class="container col-md-6 offset-3">
    <h1>Edit Post</h1>

    {{ include('post/_form.html.twig', {'button_label': 'Actualizar'}) }}

    <div class="d-flex flex-row mr-2 justify-content-sm-between mt-1">
        <a class="btn btn-primary" href="{{ path('post_index') }}">Volver</a>
        {{ include('post/_delete_form.html.twig') }}
    
    </div>

</div>
{% endblock %}
